
export const Home = () => {
    return (
        <div className="w-screen h-full relative overflow-hidden flex items-center">
            {/* Content moved to App background */}
        </div>
    );
};
